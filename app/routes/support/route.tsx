import { Support } from './Support';
import { createMeta } from '../../tools/meta';

export const meta = () =>
  createMeta({
    title: 'Support Cribbage Ninja',
    description:
      'Support Cribbage Ninja and help keep these cribbage tools up-to-date with new features.'
  });

export default Support;
